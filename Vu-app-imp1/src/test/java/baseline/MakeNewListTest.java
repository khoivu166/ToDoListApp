package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MakeNewListTest
{
    @Test
    public void testMakeNewList()
    {
        //hard code an empty list
        //call the method to make a new todo list
        //assert if the new list is is empty like the empty list
    }
}